import numpy as np

y = [100, 100, 100, -100, -100, -100, 100, 100, 100, -100, -100, -100, 100, 100, 100, -100, -100, -100, 100, 100, 100, -100, -100, -100]

def dft(n,ythis,fps):   #questionable 
    '''for z in range(len(ythis)):
        sum=0
        for t in range(0,1,fps):
            sum+=y[z]*np.exp(-1j*2*np.pi*n*t)'''
    pass

#outputfunction = dft(y)
